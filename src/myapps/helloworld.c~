#include <stdio.h>
#include "contiki.h"

void helloworld(){
	printf("Hello and welcome in our custom app. This is the 'hello world' but now comes the serial shell. Ready ?\n");
	int awaiting_user = getchar(); 
}
